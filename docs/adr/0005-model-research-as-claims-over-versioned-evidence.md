# Model research as Claims over versioned Evidence Records

Learning Artifacts are assembled from atomic Claims classified as source facts, inferences, or design recommendations, with each Claim linked to exact Evidence Records over immutable content-hashed Source Snapshots. This replaces informal citations embedded only in generated Markdown so the runtime can validate source scope, content version, exact ranges, tool-call lineage, and the boundary between upstream fact and the Agent's interpretation before publication.
